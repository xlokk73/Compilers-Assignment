build.sh builds the compiler into slc binary
test_build.sh build the compiler and runs the tests
edit.sh opens vim with the needed tabs to edit the compiler source code
fsa.jpg contains an image of the fsa for the lexer
source/ contains the source code
TODO.txt contains notes on what needs to be done next
